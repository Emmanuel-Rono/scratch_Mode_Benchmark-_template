# Reference implementation
Keep one or more reference implementations here.

For Scratch mode, the reference solution is an oracle, not necessarily the only valid artifact layout. The verifier should grade the required semantics rather than blindly compare file identity.

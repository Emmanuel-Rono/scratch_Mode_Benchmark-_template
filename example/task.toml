schema_version = "1.1"
id = "demo_scratch_json"
mode = "scratch"
category = "generic-analysis"

[paths]
instruction = "instruction.md"
environment = "environment"
solution = "solution"
tests = "tests"
output = "/output/submission.json"

[timeouts]
agent_seconds = 300
verifier_seconds = 30

[grading]
verifier = "tests/verifier.py"
pass_threshold = 0.8
